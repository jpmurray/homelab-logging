{
  "schema_version": 1,
  "profile_revision": 2,
  "name": "docker",
  "description": "Generic Docker host using container names as services",
  "journal": true,
  "required_paths": ["/var/lib/docker"],
  "files": [],
  "tasks": [],
  "docker": {
    "enabled": true,
    "mode": "api",
    "polling_interval": 5
  },
  "detect": {
    "mode": "any",
    "priority": 10,
    "probes": [
      {"type": "command", "value": "docker"},
      {"type": "path", "value": "/var/lib/docker"}
    ]
  },
  "test_service": "docker-test",
  "notes": [
    "Reads container logs through Docker's API and labels each stream with its container name.",
    "New and recreated containers are discovered automatically; container-ID path synchronization is not required.",
    "The installer adds the rsyslog-docker package required by this mode."
  ]
}
